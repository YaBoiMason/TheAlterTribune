module.exports = options => {
    options = options || {};
    let regexBase = 'n+[i1l|]+[gkq469]+[e3a4i]+[ra4]';

    return options.exact ? new RegExp(`^${regexBase}$`) : new RegExp(`\\s*${regexBase}\\s*`, 'gi');
};
